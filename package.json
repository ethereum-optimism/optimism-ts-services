{
  "name": "client-services",
  "version": "0.0.1",
  "description": "[Optimism] Client-Side Services",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "ts-node ./bin/run.ts",
    "build": "yarn run build:typescript",
    "build:typescript": "tsc -p .",
    "lint": "yarn run lint:typescript",
    "lint:typescript": "tslint --format stylish --project .",
    "lint:fix": "yarn run lint:fix:typescript",
    "lint:fix:typescript": "prettier --config prettier-config.json --write \"{src,bin,test}/**/*.ts\""
  },
  "dependencies": {
    "@eth-optimism/contracts": "^0.0.2-alpha.13",
    "colors": "^1.4.0",
    "dotenv": "^8.2.0",
    "ethers": "^5.0.22",
    "merkletreejs": "^0.2.10",
    "rlp": "^2.2.6"
  },
  "devDependencies": {
    "@types/node": "^14.14.9",
    "prettier": "^2.2.0",
    "ts-node": "^9.0.0",
    "tslint": "^6.1.3",
    "typescript": "^4.1.2"
  }
}
